services:
  db:
    image: postgres:16
    container_name: bluepill-db
    environment:
      POSTGRES_DB: bluepill
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - ./postgres_data:/var/lib/postgresql/data