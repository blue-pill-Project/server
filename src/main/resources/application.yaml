spring:
  application:
    name: server
  profiles:
    active: local
  threads:
    virtual:
      enabled: true
  config:
    import:
      - secrets.yml

springdoc:
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha
    display-request-duration: true

