spring:
  application:
    name: server
  profiles:
    active: local
  threads:
    virtual:
      enabled: true
  config:
    import:
      - optional:application-secrets.yml
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
          discord:
            client-id: ${DISCORD_CLIENT_ID}
            client-secret: ${DISCORD_CLIENT_SECRET}
            client-name: Discord
            authorization-grant-type: authorization_code
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            scope: identify,email
            provider: discord
        provider:
          discord:
            authorization-uri: https://discord.com/api/oauth2/authorize
            token-uri: https://discord.com/api/oauth2/token
            user-info-uri: https://discord.com/api/users/@me
            user-name-attribute: id

jwt:
  secret: ${JWT_SECRET}
  access-token-expiration: ${JWT_ACCESS_TOKEN_EXPIRATION:3600}
  refresh-token-expiration: ${JWT_REFRESH_TOKEN_EXPIRATION:604800}

app:
  frontend:
    oauth-callback-url: ${FRONTEND_OAUTH_CALLBACK_URL:http://localhost:5173/auth/callback}
  cookie:
    secure: ${COOKIE_SECURE:false}
    same-site: ${COOKIE_SAME_SITE:Lax}

springdoc:
  swagger-ui:
    path: /docs
    tags-sorter: alpha
    operations-sorter: alpha
    display-request-duration: true

cloudflare:
  r2:
    bucket-name: bluepill-images
    public-domain: https://pub-6197228c2b2a487daea08784bd1677d4.r2.dev
    account-id: ${CLOUDFLARE_R2_ACCOUNT_ID}
    access-key: ${CLOUDFLARE_R2_ACCESS_KEY}
    secret-key: ${CLOUDFLARE_R2_SECRET_KEY}

agent:
  base-url: ${AGENT_BASE_URL:http://localhost:8000}

# nginx 등 리버스 프록시가 붙인 X-Forwarded-* 헤더를 신뢰해서
# {baseUrl} 등 요청 URL 계산 시 실제 스킴(https)/호스트를 쓰도록 함.
# 없으면 OAuth2 redirect_uri가 내부 프록시 연결 기준(http)으로 잘못 계산됨.
server:
  forward-headers-strategy: framework
